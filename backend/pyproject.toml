[project]
name = "backend"
version = "0.1.0"
description = "Python backend for the todo-app monorepo using fastapi"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "fastapi[standard-no-fastapi-cloud-cli]>=0.138.0",
]